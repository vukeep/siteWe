# 🎬 Руководство по Hero Video

## Проблема и Решение

### Что было не так?
Hero Video не отображалось на главной странице из-за **кэширования Next.js**. Данные сохранялись в Sanity CMS, но Next.js использовал старый кэш с пустыми данными.

### Что исправлено?
✅ Сокращено время кэширования в dev режиме (с 1 часа до 10 секунд)
✅ Добавлен API endpoint для принудительной ревалидации
✅ Настроен webhook для автоматического обновления при изменениях в Sanity
✅ Добавлена кнопка "🔄 Обновить Главную" в админке Sanity
✅ Улучшено условие отображения видео

---

## Как использовать Hero Video

### Шаг 1: Откройте админку Sanity
Перейдите по адресу: http://localhost:3000/admin

### Шаг 2: Настройте Hero Video
1. Нажмите на "🏠 Главная страница"
2. Включите переключатель **"🎬 Включить Hero Video"**
3. Вставьте **базовый URL из Cloudinary** в поле "📹 Исходный URL видео"
   - Пример: `https://res.cloudinary.com/your-cloud/video/upload/v123/video.mp4`
4. Оптимизированные версии (videoUrl и posterUrl) создадутся автоматически
5. Настройте дополнительные параметры:
   - **▶️ Автозапуск** - запускать видео автоматически
   - **🔇 Без звука** - отключить звук (рекомендуется для autoplay)
   - **🔁 Зациклить** - повторять видео

### Шаг 3: Обновите страницу
Есть 3 способа обновить главную страницу:

#### Способ 1: Кнопка в Sanity Studio (Рекомендуется)
1. Нажмите кнопку **"🔄 Обновить Главную"** в правом верхнем углу
2. Дождитесь уведомления "✅ Главная страница успешно обновлена!"
3. Откройте главную страницу - видео появится немедленно

#### Способ 2: Автоматическое обновление (Dev режим)
- В dev режиме кэш обновляется каждые **10 секунд**
- Просто подождите 10 секунд и обновите страницу (F5)

#### Способ 3: API запрос
```bash
# Ревалидация по тегу
curl -X POST http://localhost:3000/api/revalidate \
  -H "Content-Type: application/json" \
  -d '{"secret":"dev-secret","tag":"homepage"}'

# Ревалидация по пути
curl -X POST http://localhost:3000/api/revalidate \
  -H "Content-Type: application/json" \
  -d '{"secret":"dev-secret","path":"/"}'
```

---

## Технические детали

### Время кэширования
- **Dev режим**: 10 секунд - для быстрого тестирования
- **Production**: 1 час - для максимальной производительности

### API Endpoints

#### 1. `/api/revalidate` - Принудительная ревалидация
**POST запрос:**
```json
{
  "secret": "dev-secret",
  "tag": "homepage"  // или "path": "/"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "✅ Tag 'homepage' revalidated successfully",
  "revalidated": true,
  "now": 1234567890
}
```

#### 2. `/api/sanity-webhook` - Webhook от Sanity
Автоматически вызывается при изменении документов в Sanity CMS.

**Поддерживаемые типы документов:**
- `homepage` - настройки главной страницы
- `portfolio` - проекты портфолио
- `lead` - заявки клиентов

---

## Настройка Webhook в Sanity (Production)

### Шаг 1: Создайте Webhook в Sanity Dashboard
1. Откройте https://sanity.io/manage
2. Выберите ваш проект
3. Перейдите в **API → Webhooks**
4. Нажмите **Add Webhook**

### Шаг 2: Заполните форму
- **Name**: Homepage Auto-Revalidation
- **URL**: `https://yourdomain.com/api/sanity-webhook?secret=YOUR_SECRET`
- **Trigger on**: Create, Update, Delete
- **Filter**: `_type == "homepage"`
- **HTTP method**: POST
- **API version**: v2021-10-21

### Шаг 3: Добавьте переменную окружения
В файл `.env.local` добавьте:
```env
SANITY_WEBHOOK_SECRET=your-secure-random-secret
```

Генерируйте безопасный секрет:
```bash
openssl rand -hex 32
```

---

## Troubleshooting

### Видео не появляется после настройки
1. ✅ Проверьте, что **heroVideoEnabled = true**
2. ✅ Проверьте, что **heroVideoUrl** и **heroPosterUrl** заполнены
3. ✅ Подождите 10 секунд и обновите страницу
4. ✅ Нажмите кнопку "🔄 Обновить Главную" в Sanity
5. ✅ Проверьте консоль браузера (F12) на наличие ошибок

### Видео не загружается (ошибка сети)
1. ✅ Проверьте, что URL из Cloudinary доступен
2. ✅ Откройте URL в новой вкладке - видео должно воспроизвестись
3. ✅ Проверьте CORS настройки Cloudinary

### Видео тормозит или долго загружается
1. ✅ Используйте оптимизированный формат: `f_auto,q_auto`
2. ✅ Проверьте размер видео (рекомендуется < 10MB)
3. ✅ Используйте adaptive bitrate streaming

---

## Структура кода

### Файлы, связанные с Hero Video

```
studiowe/
├── sanity/
│   ├── schemas/
│   │   └── homepage.ts           # Схема данных
│   ├── actions/
│   │   └── revalidateHomepage.ts # Кнопка обновления
│   ├── components/
│   │   └── CloudinaryVideoInput.tsx # Компонент ввода URL
│   └── sanity.config.ts          # Конфигурация (включает action)
├── src/
│   ├── app/
│   │   ├── page.tsx              # Главная страница (использует HeroVideoSection)
│   │   └── api/
│   │       ├── revalidate/
│   │       │   └── route.ts      # API для ручной ревалидации
│   │       └── sanity-webhook/
│   │           └── route.ts      # Webhook от Sanity
│   ├── components/
│   │   └── sections/
│   │       └── HeroVideoSection.tsx # Компонент видео
│   └── lib/
│       └── sanity/
│           └── queries.ts        # GROQ запросы (getHomepageSettings)
```

### Поток данных

```
1. Админ изменяет настройки в Sanity CMS
   ↓
2. Sanity отправляет webhook на /api/sanity-webhook
   ↓
3. Webhook вызывает revalidatePath('/') и revalidateTag('homepage')
   ↓
4. Next.js очищает кэш и запрашивает новые данные из Sanity
   ↓
5. getHomepageSettings() получает обновленные данные
   ↓
6. page.tsx перерендеривается с новыми данными
   ↓
7. HeroVideoSection отображает видео
```

---

## Performance Optimization

### Оптимизация Cloudinary URL

Используйте следующие трансформации для максимальной производительности:

#### Видео
```
https://res.cloudinary.com/your-cloud/video/upload/
  f_auto,          # Автоматический формат (WebM, MP4)
  q_auto,          # Автоматическое качество
  w_1920,          # Ширина 1920px
  h_1080,          # Высота 1080px
  c_limit          # Не увеличивать размер
/video.mp4
```

#### Постер (первый кадр)
```
https://res.cloudinary.com/your-cloud/video/upload/
  so_0,            # Первый кадр (0 секунд)
  f_webp,          # Формат WebP
  q_auto,          # Автоматическое качество
  w_1920,h_1080,c_limit
/video.webp
```

### Производительность компонента

HeroVideoSection использует:
- ✅ **Intersection Observer** - запускает видео только при появлении в viewport
- ✅ **Lazy loading** - постер загружается сразу, видео - при необходимости
- ✅ **Native video player** - без тяжелых библиотек
- ✅ **playsInline** - видео воспроизводится inline на мобильных устройствах

---

## Заключение

Hero Video теперь работает корректно! Все изменения в Sanity CMS будут автоматически отображаться на сайте.

**Быстрое обновление в dev режиме:**
1. Измените настройки в Sanity
2. Нажмите "🔄 Обновить Главную"
3. Обновите страницу (F5)
4. Готово! 🎉

**Автоматическое обновление в production:**
1. Настройте webhook в Sanity Dashboard
2. Изменения будут применяться автоматически в течение нескольких секунд

---

**Автор:** StudioWe Development Team  
**Дата:** 2024  
**Версия:** 1.0

