# npm Configuration for Vercel Build
# Required for Sanity CMS dependencies with React 19

# Используем legacy режим для разрешения peer dependencies конфликтов
legacy-peer-deps=true

# Автоматически использовать exact версии
save-exact=false

