# 💰 Руководство по управлению тарифами

Полное руководство по настройке и управлению тарифами через админку Sanity.

## 🚀 Быстрый старт

### 1. Заполнить начальные данные

Запустите скрипт для создания базовых тарифов:

```bash
npm run seed-pricing
```

Этот скрипт создаст:
- ✅ 3 тарифных плана (Start, Growth, Maximum)
- ✅ Настройки секции тарифов (заголовки, базовая стоимость)

### 2. Открыть админку

Перейдите в Sanity Studio: **http://localhost:3000/admin**

## 📋 Управление тарифными планами

### Где находится

**Админка → Тарифные планы** (`/admin/structure/pricingPlan`)

### Поля тарифного плана

| Поле | Описание | Пример |
|------|----------|--------|
| 📦 Название пакета | Название тарифа | `Start`, `Growth`, `Maximum` |
| 🔢 Порядок отображения | Позиция карточки (1 = слева) | `1`, `2`, `3` |
| 🎬 Количество роликов | Сколько видео входит | `До 5 роликов` |
| 💰 Цена (в рублях) | Полная стоимость | `100000` (100 тыс. ₽) |
| ⏱️ Срок выполнения | Время производства | `3 рабочих дня` |
| ✅ Что входит в пакет | Список особенностей | `[Единый стиль, Один раунд правок...]` |
| ⭐ Рекомендуемый пакет | Выделить синей рамкой | `true/false` |
| 👁️ Опубликован | Показывать на сайте | `true/false` |

### Примеры действий

#### Изменить цену пакета

1. Откройте нужный тариф (например, `Start`)
2. Найдите поле **💰 Цена (в рублях)**
3. Измените значение (например, `100000` → `120000`)
4. Нажмите **Publish**
5. Обновится автоматически через 1 час (или мгновенно через webhook)

#### Добавить новый пакет

1. Нажмите **+ Create** в разделе "Тарифные планы"
2. Заполните все поля:
   - Название: `Premium`
   - Порядок: `4`
   - Количество роликов: `До 100 роликов`
   - Цена: `1500000`
   - Срок: `14 рабочих дней`
   - Особенности: добавьте несколько пунктов
3. Нажмите **Publish**

#### Сделать пакет рекомендуемым

1. Откройте нужный тариф
2. Найдите переключатель **⭐ Рекомендуемый пакет**
3. Включите его
4. **Важно:** Выключите у других пакетов (рекомендуемый должен быть один)
5. Нажмите **Publish**

#### Скрыть пакет с сайта

1. Откройте тариф, который нужно скрыть
2. Выключите переключатель **👁️ Опубликован**
3. Нажмите **Publish**
4. Пакет исчезнет с сайта

## ⚙️ Настройки секции тарифов

### Где находится

**Админка → Настройки секции "Стоимость"** (`/admin/structure/pricingSettings`)

**⚠️ Важно:** Это singleton документ - он всегда один. Просто редактируйте его.

### Поля настроек

#### Заголовки и описания

- **📋 Заголовок секции** - Основной заголовок (`Стоимость`)
- **📝 Подзаголовок** - Описание под заголовком

#### Базовая стоимость

- **💰 Базовая стоимость за 10 секунд** - Цена в рублях (`10000`)
- **📄 Описание базовой стоимости** - Текст перед ценой (`1 ролик: каждые 10 секунд =`)

#### Индивидуальные проекты

Секция внизу страницы для нестандартных заказов:

- **🎨 Заголовок** - `Индивидуальные проекты`
- **📝 Описание** - Многострочное описание (поддерживает `\n` для переносов)
- **⭐ Выделенный текст** - Яркий акцент (`💰 Стоимость ниже...`)
- **🔘 Текст кнопки** - `Получить точный расчёт`

### Примеры действий

#### Изменить базовую стоимость

1. Откройте **Настройки секции "Стоимость"**
2. Найдите **💰 Базовая стоимость за 10 секунд**
3. Измените значение (например, `10000` → `12000`)
4. Нажмите **Publish**
5. На сайте обновится блок с базовой ценой

#### Изменить заголовок секции

1. Откройте настройки
2. Измените **📋 Заголовок секции** (например, `Тарифы и цены`)
3. Нажмите **Publish**

## 🎯 Типичные сценарии

### Сезонная акция (-20%)

```
1. Откройте все 3 тарифа
2. Умножьте цены на 0.8:
   - Start: 100000 → 80000
   - Growth: 300000 → 240000
   - Maximum: 700000 → 560000
3. Добавьте в название: "Start (Акция -20%)"
4. Опубликуйте все изменения
```

### Новый VIP пакет

```
1. Создайте новый тариф:
   - Название: VIP
   - Порядок: 4 (будет справа)
   - Количество: До 200 роликов
   - Цена: 3000000
   - Срок: 21 рабочий день
   - Рекомендуемый: false
2. Добавьте премиум-особенности
3. Опубликуйте
```

### Изменить все тексты на английский

```
1. Откройте Настройки секции
2. Измените все текстовые поля на английский:
   - Заголовок: "Pricing"
   - Подзаголовок: "Transparent pricing..."
   - И так далее
3. Откройте каждый тариф и переведите тексты
4. Опубликуйте все изменения
```

## 🔄 Синхронизация с сайтом

### Автоматическое обновление

- **ISR кеш:** 1 час (3600 секунд)
- **Webhook:** мгновенно (если настроен)

### Принудительное обновление

1. Пересоберите сайт: `npm run build`
2. Или перезапустите dev-сервер: `npm run dev`

## 📊 Формат данных

### Структура тарифного плана

```typescript
interface PricingPlan {
  id: string                // Уникальный ID
  name: string              // Название пакета
  videoCount: string        // Количество роликов
  price: number             // Цена в рублях
  duration: string          // Срок выполнения
  features: string[]        // Список особенностей
  recommended: boolean      // Рекомендуемый?
  order: number             // Порядок отображения
  published: boolean        // Опубликован?
}
```

### Пример JSON

```json
{
  "name": "Start",
  "order": 1,
  "videoCount": "До 5 роликов",
  "price": 100000,
  "duration": "3 рабочих дня",
  "features": [
    "Длительность до 30 секунд",
    "Единый стиль",
    "Один раунд правок"
  ],
  "recommended": false,
  "published": true
}
```

## ❓ FAQ

### Можно ли добавить больше 3 тарифов?

Да! Создавайте сколько угодно. Просто увеличивайте значение **Порядок отображения**.

### Как изменить валюту с ₽ на $?

1. Откройте `src/components/sections/PricingSection.tsx`
2. Найдите `.toLocaleString('ru-RU')`
3. Замените на `.toLocaleString('en-US')`
4. Измените символ `₽` на `$`

### Как добавить иконки в особенности пакета?

Просто добавьте эмодзи в текст:
- `✅ Длительность до 30 секунд`
- `🎬 Единый стиль`
- `⚡ Быстрая доставка`

### Тарифы не обновляются на сайте

1. Проверьте, что нажали **Publish** в админке
2. Подождите до 1 часа (ISR кеш)
3. Или принудительно пересоберите сайт

## 🛠️ Разработка

### Добавить новое поле в тариф

1. Откройте `sanity/schemas/pricingPlan.ts`
2. Добавьте новое поле через `defineField()`
3. Обновите интерфейс в `src/lib/sanity/queries.ts`
4. Используйте в `PricingSection.tsx`

### Изменить цвета выделения

Откройте `PricingSection.tsx` и найдите классы:
- `border-blue-600` - рамка рекомендуемого пакета
- `bg-button` - цвет кнопки (управляется из админки)

---

**🎉 Готово!** Теперь вы можете полностью управлять тарифами через админку.

