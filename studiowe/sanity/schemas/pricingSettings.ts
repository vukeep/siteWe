/**
 * Sanity Schema: Pricing Settings (Настройки секции тарифов)
 * 
 * Singleton документ для управления общими настройками секции "Стоимость"
 */

import { defineType, defineField } from 'sanity'

export default defineType({
  name: 'pricingSettings',
  title: 'Настройки секции "Стоимость"',
  type: 'document',
  fields: [
    // ========================================
    // ЗАГОЛОВКИ И ОПИСАНИЯ
    // ========================================
    defineField({
      name: 'title',
      title: '📋 Заголовок секции',
      type: 'string',
      description: 'Основной заголовок секции тарифов',
      initialValue: 'Стоимость',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'subtitle',
      title: '📝 Подзаголовок',
      type: 'text',
      description: 'Описание под заголовком',
      initialValue: 'Прозрачное ценообразование для любого объема видеопроизводства',
      validation: (Rule) => Rule.required().max(200),
    }),
    
    // ========================================
    // БАЗОВАЯ СТОИМОСТЬ
    // ========================================
    defineField({
      name: 'basePricePerTenSeconds',
      title: '💰 Базовая стоимость за 10 секунд',
      type: 'number',
      description: 'Стоимость 10 секунд видео в рублях (например: 10000)',
      initialValue: 10000,
      validation: (Rule) => Rule.required().min(0).integer(),
    }),
    defineField({
      name: 'basePriceDescription',
      title: '📄 Описание базовой стоимости',
      type: 'string',
      description: 'Текст для блока базовой стоимости',
      initialValue: '1 ролик: каждые 10 секунд =',
    }),
    
    // ========================================
    // ИНДИВИДУАЛЬНЫЕ ПРОЕКТЫ
    // ========================================
    defineField({
      name: 'customProjectTitle',
      title: '🎨 Заголовок для индивидуальных проектов',
      type: 'string',
      initialValue: 'Индивидуальные проекты',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'customProjectDescription',
      title: '📝 Описание индивидуальных проектов',
      type: 'text',
      initialValue: 'Можем изготовить ролик со сложной концепцией любой длительности.\nСроки и стоимость рассчитываются индивидуально.',
      validation: (Rule) => Rule.required().max(300),
    }),
    defineField({
      name: 'customProjectHighlight',
      title: '⭐ Выделенный текст',
      type: 'string',
      initialValue: '💰 Стоимость ниже классического продакшена в десятки раз!',
      description: 'Яркий текст для привлечения внимания',
    }),
    defineField({
      name: 'customProjectButtonText',
      title: '🔘 Текст кнопки',
      type: 'string',
      initialValue: 'Получить точный расчёт',
      validation: (Rule) => Rule.required(),
    }),
    
    // ========================================
    // МЕТА
    // ========================================
    defineField({
      name: 'lastModified',
      title: 'Последнее изменение',
      type: 'datetime',
      readOnly: true,
      initialValue: () => new Date().toISOString(),
    }),
  ],
  preview: {
    prepare() {
      return {
        title: '💰 Настройки секции "Стоимость"',
        subtitle: 'Заголовки, базовая стоимость, индивидуальные проекты',
      }
    },
  },
})

