# 🔧 Hero Video - Сводка Исправлений

## Проблема
Hero Video не отображался на главной странице, хотя все ссылки были заполнены в админке Sanity.

### Причина
**Кэширование Next.js ISR (Incremental Static Regeneration)**
- Данные сохранялись в Sanity CMS корректно
- Next.js кэшировал запросы на 1 час (`revalidate: 3600`)
- При первой загрузке данных не было (null)
- Кэш не обновлялся автоматически при изменении данных в Sanity

---

## Исправления

### 1. ⚡ Уменьшено время кэширования в dev режиме
**Файл:** `src/lib/sanity/queries.ts`

```typescript
// До:
revalidate: 3600  // 1 час для всех режимов

// После:
const revalidateTime = process.env.NODE_ENV === 'development' ? 10 : 3600
revalidate: revalidateTime  // Dev: 10 сек, Prod: 1 час
```

**Эффект:** В dev режиме обновление каждые 10 секунд вместо 1 часа

---

### 2. 🔄 Добавлен API для принудительной ревалидации
**Новый файл:** `src/app/api/revalidate/route.ts`

Позволяет вручную очистить кэш через API запрос:

```bash
curl -X POST http://localhost:3000/api/revalidate \
  -H "Content-Type: application/json" \
  -d '{"secret":"dev-secret","tag":"homepage"}'
```

**Функциональность:**
- ✅ Ревалидация по тегу (`homepage`, `portfolio`, etc.)
- ✅ Ревалидация по пути (`/`, `/portfolio`, etc.)
- ✅ Защита секретным токеном
- ✅ Совместимость с Next.js 16 (второй аргумент `profile`)

---

### 3. 🪝 Обновлен webhook для поддержки homepage
**Файл:** `src/app/api/sanity-webhook/route.ts`

Добавлена обработка изменений документа `homepage`:

```typescript
if (_type === 'homepage') {
  revalidatePath('/', 'page')
  revalidateTag('homepage', 'max')  // Next.js 16 требует второй аргумент
  // ...
}
```

**Эффект:** Автоматическое обновление главной страницы при изменении настроек в Sanity

---

### 4. 🔘 Добавлена кнопка "Обновить Главную" в Sanity Studio
**Новый файл:** `sanity/actions/revalidateHomepage.ts`
**Обновлен:** `sanity/sanity.config.ts`

Добавлено кастомное действие для документа `homepage`:

```typescript
document: {
  actions: (prev, context) => {
    if (context.schemaType === 'homepage') {
      return [...prev, revalidateHomepageAction]
    }
    return prev
  }
}
```

**Эффект:** Админ может обновить страницу одним кликом прямо из Sanity Studio

---

### 5. ✅ Улучшено условие отображения видео
**Файл:** `src/app/page.tsx`

```typescript
// До:
{homepageSettings?.heroVideoEnabled && 
 homepageSettings.heroVideoUrl && 
 homepageSettings.heroPosterUrl && (
  <HeroVideoSection ... />
)}

// После:
{homepageSettings?.heroVideoEnabled && 
 homepageSettings?.heroVideoUrl && 
 homepageSettings?.heroPosterUrl ? (
  <HeroVideoSection ... />
) : process.env.NODE_ENV === 'development' ? (
  <DevNotice />
) : null}
```

**Эффект:** 
- Более явная логика отображения
- Dev notice показывается только в development
- Лучшая обработка null значений

---

### 6. 🐛 Исправлена совместимость с Next.js 16
**Проблема:** `revalidateTag` требует 2 аргумента в Next.js 16+

**Решение:** Добавлен второй аргумент `'max'` во всех местах:
```typescript
// До (ошибка):
revalidateTag('homepage')

// После (работает):
revalidateTag('homepage', 'max')
```

**Затронутые файлы:**
- ✅ `src/app/api/revalidate/route.ts`
- ✅ `src/app/api/sanity-webhook/route.ts`

---

## Новые файлы

```
studiowe/
├── sanity/actions/
│   └── revalidateHomepage.ts         # ⭐ NEW - Кнопка обновления в Sanity
├── src/app/api/revalidate/
│   └── route.ts                      # ⭐ NEW - API ревалидации
├── HERO_VIDEO_GUIDE.md               # ⭐ NEW - Полная документация
└── HERO_VIDEO_FIX_SUMMARY.md         # ⭐ NEW - Эта сводка
```

---

## Измененные файлы

```
studiowe/
├── sanity/
│   └── sanity.config.ts              # ✏️ MODIFIED - Добавлен document.actions
├── src/
│   ├── app/
│   │   ├── page.tsx                  # ✏️ MODIFIED - Улучшено условие
│   │   └── api/sanity-webhook/
│   │       └── route.ts              # ✏️ MODIFIED - Добавлена обработка homepage
│   └── lib/sanity/
│       └── queries.ts                # ✏️ MODIFIED - Динамическое время кэша
```

---

## Как использовать

### Вариант 1: Автоматическое обновление (Dev)
1. Измените настройки в Sanity
2. Подождите 10 секунд
3. Обновите страницу (F5)

### Вариант 2: Кнопка в Sanity (Рекомендуется)
1. Измените настройки в Sanity
2. Нажмите "🔄 Обновить Главную"
3. Обновите страницу (F5)

### Вариант 3: API запрос
```bash
curl -X POST http://localhost:3000/api/revalidate \
  -H "Content-Type: application/json" \
  -d '{"secret":"dev-secret","tag":"homepage"}'
```

---

## Настройка для Production

### 1. Добавьте переменные окружения
```env
# .env.local или .env.production
REVALIDATE_SECRET=your-secure-random-secret
NEXT_PUBLIC_REVALIDATE_SECRET=your-secure-random-secret
SANITY_WEBHOOK_SECRET=your-webhook-secret
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
```

### 2. Настройте Webhook в Sanity Dashboard
1. Откройте https://sanity.io/manage
2. API → Webhooks → Add Webhook
3. URL: `https://yourdomain.com/api/sanity-webhook?secret=YOUR_SECRET`
4. Trigger: Create, Update, Delete
5. Filter: `_type == "homepage"`

---

## Производительность

### Время обновления данных

| Режим | Метод | Время |
|-------|-------|-------|
| Development | Автоматическое | 10 сек |
| Development | Кнопка в Sanity | Немедленно |
| Development | API запрос | Немедленно |
| Production | Webhook | 1-3 сек |
| Production | API запрос | Немедленно |
| Production | Автоматическое | 1 час |

### Оптимизации

- ✅ **ISR (Incremental Static Regeneration)** - страницы генерируются statically
- ✅ **Tag-based revalidation** - точечное обновление только нужных данных
- ✅ **Path-based revalidation** - ревалидация конкретных страниц
- ✅ **Cloudinary auto-optimization** - автоматическая оптимизация видео

---

## Результат

✅ **Hero Video теперь работает корректно**
✅ **Изменения в Sanity применяются за 10 секунд (dev) или через webhook (prod)**
✅ **Добавлена кнопка для мгновенного обновления**
✅ **Все TypeScript ошибки исправлены**
✅ **Совместимость с Next.js 16**
✅ **Полная документация создана**

---

## Debug информация

### Проверка текущего состояния

Откройте терминал с Next.js dev server и найдите строки:

```
📊 Homepage Settings: {
  exists: true,        # ✅ Должно быть true
  enabled: true,       # ✅ Должно быть true
  hasVideoUrl: true,   # ✅ Должно быть true
  hasPosterUrl: true,  # ✅ Должно быть true
  data: { ... }        # ✅ Должны быть данные
}
```

Если `exists: false` - данные еще в кэше, подождите 10 секунд.

### Консоль браузера (F12)

Если видео не загружается, проверьте:
1. Network tab - должен быть запрос к Cloudinary
2. Console tab - не должно быть ошибок CORS
3. Elements tab - должен быть `<video>` элемент

---

**Статус:** ✅ Все проблемы исправлены, Hero Video работает  
**Версия Next.js:** 16.0.7  
**Версия Sanity:** 3.x  
**Дата:** Декабрь 2024

