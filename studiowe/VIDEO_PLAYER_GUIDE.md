# Руководство по Video Player

## 🎯 Текущая Реализация

StudioWe использует **улучшенный нативный HTML5 video player** с автоопределением ориентации видео.

---

## ✨ Функции

### 1️⃣ **Автоопределение Ориентации**

Плеер автоматически определяет ориентацию видео из метаданных:

```typescript
// Типы ориентации:
- 'vertical' → 9:16, 4:5 (соцсети, stories)
- 'horizontal' → 16:9, 21:9 (YouTube, кино)
- 'square' → 1:1 (Instagram квадрат)
```

**Как это работает:**
1. При загрузке видео читаются метаданные (`videoWidth` / `videoHeight`)
2. Вычисляется aspect ratio
3. Модальное окно автоматически подстраивается:
   - **Узкое** (max-w-2xl) для вертикальных
   - **Широкое** (max-w-6xl) для горизонтальных

---

### 2️⃣ **Адаптивное Модальное Окно**

```tsx
// Модальное окно изменяет ширину в зависимости от ориентации
<div className={`
  relative w-full transition-all duration-300 
  ${videoOrientation === 'vertical' ? 'max-w-2xl' : 'max-w-6xl'}
`}>
```

**Результат:**
- 📱 Вертикальные видео (9:16) → узкое окно
- 🖥️ Горизонтальные видео (16:9) → широкое окно
- ⏹️ Квадратные видео (1:1) → среднее окно

---

### 3️⃣ **Lazy Loading**

Видео загружается **только после клика** на карточку:

```typescript
const [showVideo, setShowVideo] = useState(false)

// Видео создается только когда showVideo = true
{showVideo && (
  <video src={videoUrl} autoPlay />
)}
```

**Преимущества:**
- ⚡ Быстрая загрузка страницы
- 💰 Экономия трафика
- 🚀 Лучшие Core Web Vitals

---

### 4️⃣ **Оптимизация Постеров**

Постеры оптимизированы через Next.js Image:

```tsx
<Image
  src={posterUrl}
  priority={priority} // Первая карточка загружается сразу
  sizes="..." // Адаптивные размеры
/>
```

---

## 🔧 Как Использовать

### Базовое Использование

```tsx
import { VideoPlayer } from '@/components/ui/VideoPlayer'

<VideoPlayer
  videoUrl="https://res.cloudinary.com/.../video.mp4"
  posterUrl="https://res.cloudinary.com/.../poster.jpg"
  title="Название видео"
  controls
  autoplay
  aspectRatio="auto" // Автоопределение
/>
```

### С Обратным Вызовом

```tsx
const [orientation, setOrientation] = useState<'vertical' | 'horizontal' | null>(null)

<VideoPlayer
  // ...props
  aspectRatio="auto"
  onAspectRatioDetected={(isVertical, ratio) => {
    setOrientation(isVertical ? 'vertical' : 'horizontal')
    console.log('Aspect ratio:', ratio)
  }}
/>
```

---

## 📊 Логи в Консоли

При загрузке видео вы увидите:

```
📹 Видео метаданные: {
  width: 1080,
  height: 1920,
  ratio: "0.56",
  orientation: "vertical"
}
```

Это помогает отлаживать и проверять ориентацию видео.

---

## 🎨 Стили

### Постер (до клика)
```css
object-cover /* Заполняет контейнер, обрезка по краям */
```

### Видео (после клика)
```css
object-contain /* Видео полностью видно, черные полосы если нужно */
bg-black /* Черный фон вместо белых полос */
```

---

## 🚀 Производительность

| Метрика | Значение |
|---------|----------|
| Bundle Size | **0 KB** (нативный) |
| Время загрузки | **Мгновенно** |
| Memory | **Минимальное** |
| Core Web Vitals | **Отлично** |

---

## 🔄 Миграция на Cloudinary Video Player (опционально)

См. `VIDEO_PLAYERS_COMPARISON.md` для детального сравнения и инструкций по миграции.

**Когда нужно:**
- Adaptive streaming
- Аналитика просмотров
- Профессиональный плеер
- Субтитры и многоязычность

---

## ✅ Чеклист Проверки

При добавлении нового видео проверьте:

- [ ] URL корректен (без пробелов)
- [ ] Постер существует
- [ ] Видео загружается и воспроизводится
- [ ] Ориентация определяется правильно
- [ ] Модальное окно адаптируется

---

## 💡 Советы

1. **Формат видео:** MP4 (H.264) для лучшей совместимости
2. **Размер файла:** До 50MB для быстрой загрузки
3. **Длительность:** До 2-3 минут для портфолио
4. **Качество:** 1080p достаточно для веба

---

## 🐛 Troubleshooting

### Видео не воспроизводится
- Проверьте URL (нет лишних пробелов)
- Проверьте формат (должен быть MP4)
- Откройте консоль браузера для ошибок

### Неправильная ориентация
- Проверьте логи в консоли
- Убедитесь что `aspectRatio="auto"`
- Проверьте метаданные файла

### Медленная загрузка
- Оптимизируйте размер файла в Cloudinary
- Используйте Cloudinary трансформации
- Рассмотрите Cloudinary Video Player для streaming

